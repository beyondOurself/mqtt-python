# 需求调研报告 — MQTT 发送工具

> 用于沉淀需求来源、市场信息、竞品对比与关键问题结论，为需求立项和方案选择提供证据。
> 与 PRD.md（需求定义）、TECH_DESIGN.md（技术设计）联动，作为“为什么这样做”的依据文档。
> 仅记录调研事实、分析过程与结论，**不存放任务排期和长期工程规范**。

## 一、项目背景与目标

博享家 IoT / 智慧通行链路依赖 MQTT 报文（如 `channelPersonAlert`、`heartBeat`、`timing`、`banner`）。联调 GIS、业主端、物业端、设备时，需要把带 `muid` + `sign` 的 JSON 发到指定 broker / topic。

本仓库是内部桌面工具：在 Windows 上填 Host、Topic、账号，选模版后一键发送，避免每次改脚本、算签名。

## 二、市场现状分析

- MQTTX、MQTT Explorer 适合通用订阅/发布，不内置博享家 `md5sign`（key 排序 + timestamp 偏移 + UTF-8 MD5）。
- 纯脚本（`mqtt_publish.py`）能发，但切设备 SN、改 payload、留历史成本高。
- 内部已有 Tkinter GUI（`mqtt_gui.py`）+ PyInstaller 单文件 exe（`mqtt_tool_v5`），日常在用。

## 三、竞品对比

| 工具 | 通用 MQTT | 博享家签名 | 模版/历史 | 离线单文件 |
|------|-----------|------------|-----------|------------|
| MQTTX | 强 | 无 | 有 | 否（需安装） |
| 本工具 | 仅发布 | 有 | 有 | 有 |

结论：不替代 MQTTX；定位「设备上行报文模拟器」。

## 四、用户需求洞察

- 联调人员要：改 SN / 主题、选现成报文、发送前自动 `muid` + `sign`、通道告警时间取当前。
- 模版与 Host/Topic 历史要落盘，exe 与源码旁各一份 JSON。
- 密码默认掩码，可临时显示；不把密码写入 `history.json`。
- **多场景并行**：多 Tab JSON、Tab 拖出独立窗口、窗口名可改，避免多窗混淆。
- **模版管理**：弹窗内完整 CRUD、JSON 直接编辑、粘贴即格式化、改错可回撤操作记录。
- **开箱即用**：无 Python 环境时一键 `setup.bat`；开发用 `dev.bat` 热重载。

## 五、风险与约束

- 源码曾硬编码测试账号口令，禁止再入库、禁止写进文档。
- `md5sign` 把 `timestamp` 设为当前毫秒减 4 分钟，与现网校验对齐，勿擅自改偏移。
- 仅发布、不订阅；QoS/TLS 未做。
- Windows 为主；依赖 `paho-mqtt` + 标准库 Tkinter。

## 六、结论与建议

- 以现有 GUI 为产品本体，脚本作调试备用。
- MVP：发布 + 签名 + 模版 CRUD + 连接历史 + 日志 + 多 Tab/分离窗 + 模版管理弹窗 + 环境脚本。
- 后续：去硬编码口令、订阅回显、TLS/QoS。
